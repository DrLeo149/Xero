# ----------------------------------------------------------------------------
# Copy this file to `backend/.env` and fill in real values. Never commit .env.
# ----------------------------------------------------------------------------

# Xero API credentials (from developer.xero.com)
XERO_CLIENT_ID=
XERO_CLIENT_SECRET=
XERO_REDIRECT_URI=http://localhost:3000/api/xero/callback

# Xero OAuth scopes (space-separated)
XERO_SCOPES=openid profile email offline_access accounting.transactions.read accounting.contacts.read accounting.reports.read accounting.settings.read

# Database
# Local dev: spin up Postgres with `docker compose up -d` from repo root
DATABASE_URL="postgresql://mynumbers:mynumbers_dev@localhost:5432/mynumbers"
# Production (Railway): auto-injected by Postgres plugin

# JWT signing secret (generate with: openssl rand -hex 64)
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d

# Initial admin account seeded on first boot
SEED_ADMIN_EMAIL=adarsh@ankyadvisors.com
SEED_ADMIN_PASSWORD=change-me-on-first-login

# Server
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173
